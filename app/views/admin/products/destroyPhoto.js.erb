<% if ["detail", "color", "point", "form", "wrap", "wash"].include?(@photo.img_type) %>
$('<%= "#photo_#{@photo.img_type}_#{@photo.id}" %>').remove();

if($('<%= "[id^=photo_#{@photo.img_type}]" %>').length == 0){
	$('<%= "#no_photo_#{@photo.img_type}" %>').show();
}
<% end %>